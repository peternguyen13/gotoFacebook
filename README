#################################################################
#			gotoFacebook				#
#		Writen by Peter Nguyen (NVH)			#
#################################################################

GotoFacebook using C:
  Linux:
    make
    if you want to install gotoFacebook on your computer, you should type:
	---> sudo make install    
        ---> sudo gotoFacebook
    or you can run it portable:
    sudo ./gotoFacebook
  Windows:
    Run gotoFacebook as Administrator for Windows 7
    open with C-Free, config line : link_hosts="C:\Windows\System32\drivers\etc\hosts
    click menu build, choose Build -> Build
    Binary for Windows + list file ip: http://www.mediafire.com/?l6zyc01yfv1su
  Usage: <option> <sources of file>
	  -U: update list file
	  -R: restore list file
	  -B: backup host file
  Warning:
	  You should run gotoFacebook -B first to backup your host file
	  In case, hosts file have problem: 
	  - Ip host was fail, you can use gotoFacebook -R <link to backup file>
	  - Internet service doesn't block facebook, you can use gotoFacebook -R <link to backup file>
	  Have fun
GotoFacebook using python:
  edit: dir_host="list" => dir_host=<link to list(file)>
  chmod u+x gotoFacebook.py
  sudo ./gotoFacebook.py